spring:
  datasource:
    url: ${NAIS_DATABASE_KABAL_SMART_EDITOR_API_PRIV_KABAL_SMART_EDITOR_API_JDBC_URL}
    hikari:
      maximum-pool-size: 10

springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false
